#企业库前台
server {
	listen 80;
	server_name company.gongchang.net;

	location ~* /info/([0-9]+)_([0-9a-z]+)/ {
        proxy_pass 192.168.8.165:8000;
    }
    location ~* /(p-([0-9]+)/([0-9]+)_([0-9a-z]+))/ {
        proxy_pass 192.168.8.165:81;
    }
    location ~* /sc(.*) {
        proxy_pass http://search.company.gc.com;
    }
    location / {
    	 proxy_pass 192.168.8.165:8000;
    }
}